import React from 'react';

const NotFoundPage = () => {
    return (
        <div style={{padding: '40px, 0px', margin: 'auto'}}>
            <p>Data Not Found</p>
        </div>
    );
};

export default NotFoundPage;